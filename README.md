# IT Resource Manager — Server

This workspace provides a minimal Flask server to serve the existing static front-end and example API endpoints.

Quick start (macOS):

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

Open http://127.0.0.1:5000 in your browser.
# it-resource
# it-resource
# it-resource
